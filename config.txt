import socket
import threading
import time

# -------------------------------
# Simple Firewall Simulation Code
# -------------------------------

# Define blocked IPs and suspicious keywords
BLOCKED_IPS = {"192.168.1.50", "10.0.0.99"}
SUSPICIOUS_KEYWORDS = ["malware", "attack", "phishing", "exploit"]

# Log file
LOG_FILE = "firewall_log.txt"

def log_event(event):
    """Log firewall events to a file with timestamp."""
    with open(LOG_FILE, "a") as f:
        f.write(f"[{time.strftime('%Y-%m-%d %H:%M:%S')}] {event}\n")

def check_packet(ip, data):
    """Check incoming packet for suspicious activity."""
    if ip in BLOCKED_IPS:
        log_event(f"Blocked packet from {ip}")
        return False
    for keyword in SUSPICIOUS_KEYWORDS:
        if keyword in data.lower():
            log_event(f"Suspicious content detected from {ip}: {keyword}")
            return False
    return True

def handle_client(client_socket, client_address):
    """Handle incoming client connection."""
    ip = client_address[0]
    try:
        while True:
            data = client_socket.recv(1024).decode("utf-8")
            if not data:
                break
            if check_packet(ip, data):
                log_event(f"Allowed traffic from {ip}: {data}")
                client_socket.send("Packet allowed\n".encode("utf-8"))
            else:
                client_socket.send("Packet blocked\n".encode("utf-8"))
    except Exception as e:
        log_event(f"Error handling client {ip}: {e}")
    finally:
        client_socket.close()

def start_firewall(host="0.0.0.0", port=9999):
    """Start the firewall server."""
    server = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    server.bind((host, port))
    server.listen(5)
    log_event(f"Firewall started on {host}:{port}")
    print(f"Firewall running on {host}:{port}...")

    while True:
        client_socket, client_address = server.accept()
        log_event(f"Connection attempt from {client_address[0]}")
        client_handler = threading.Thread(
            target=handle_client, args=(client_socket, client_address)
        )
        client_handler.start()

if __name__ == "__main__":
    start_firewall()
API_KEY=THM{super_secret_key1234}
import os

# List of suspicious file extensions often used by malware
SUSPICIOUS_EXTENSIONS = [".exe", ".bat", ".scr", ".js", ".vbs"]

def scan_directory(path):
    print(f"Scanning directory: {path}")
    for root, dirs, files in os.walk(path):
        for file in files:
            for ext in SUSPICIOUS_EXTENSIONS:
                if file.lower().endswith(ext):
                    print(f"[!] Suspicious file detected: {os.path.join(root, file)}")
# Example: Long Python script combining file handling, networking, and data analysis

import os
import requests
import csv
import statistics

# Step 1: Download sample data
url = "https://people.sc.fsu.edu/~jburkardt/data/csv/airtravel.csv"
response = requests.get(url)
filename = "airtravel.csv"

with open(filename, "wb") as f:
    f.write(response.content)

print("Data downloaded successfully!")

# Step 2: Read CSV data
data = []
with open(filename, newline="") as csvfile:
    reader = csv.reader(csvfile)
    header = next(reader)
    for row in reader:
        data.append([int(x) for x in row[1:]])

print("Data loaded:", header)

# Step 3: Perform analysis
monthly_totals = [sum(month) for month in zip(*data)]
average_passengers = statistics.mean(monthly_totals)

print("Monthly totals:", monthly_totals)
print("Average passengers per month:", average_passengers)

# Step 4: Save results
with open("analysis_results.txt", "w") as f:
    f.write("Monthly totals: " + str(monthly_totals) + "\n")
    f.write("Average passengers: " + str(average_passengers) + "\n")

print("Analysis saved to analysis_results.txt")
#!/usr/bin/env python3
"""
Cybersecurity Log Analyzer
Author: Example
Purpose: Parse system logs, detect suspicious activity, and generate a report.
"""
API_KEY=THM{super_secret_key123}

print("Sniffer started... Press Ctrl+C to stop.")

try:
    while True:
        raw_data, addr = sniffer.recvfrom(65535)
        dest_mac, src_mac, eth_proto, data = ethernet_frame(raw_data)
        print(f"\nEthernet Frame: {src_mac} -> {dest_mac}, Protocol: {eth_proto}")

        # IPv4 packets
        if eth_proto == 8:
            ttl, proto, src, target, data = ipv4_packet(data)
            print(f"IPv4 Packet: {src} -> {target}, TTL: {ttl}, Protocol: {proto}")

            # Detect suspicious traffic (example: unusual TTL or ICMP flood)
            if ttl < 5:
                print("⚠️ Suspicious: Very low TTL (possible crafted packet)")
            if proto == 1:  # ICMP
                print("⚠️ ICMP traffic detected (possible ping flood)")
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Participant Registration Form</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f4f6f9;
      margin: 0;
      padding: 20px;
    }
    .form-container {
      background: #fff;
      padding: 25px;
      border-radius: 8px;
      max-width: 500px;
      margin: auto;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    }
    h2 {
      text-align: center;
      color: #333;
    }
    label {
      display: block;
      margin-top: 15px;
      font-weight: bold;
    }
    input[type="text"], input[type="email"] {
      width: 100%;
      padding: 10px;
      margin-top: 5px;
      border: 1px solid #ccc;
      border-radius: 4px;
    }
    .note {
      font-size: 0.9em;
      color: #666;
    }
    .error {
      color: red;
      font-size: 0.9em;
      margin-top: 5px;
    }
    button {
      margin-top: 20px;
      width: 100%;
      padding: 12px;
      background: #0078d7;
      color: #fff;
      border: none;
      border-radius: 4px;
      font-size: 16px;
      cursor: pointer;
    }
    button:hover {
      background: #005a9e;
    }
  </style>
</head>
<body>
  <div class="form-container">
    <h2>Event Registration</h2>
    <form id="registrationForm">
      <label for="name">Full Name:</label>
      <input type="text" id="name" name="name" placeholder="Enter your full name" required>

      <label for="collegeEmail">Please enter your official college email address:</label>
      <input type="email" id="collegeEmail" name="collegeEmail" placeholder="example@college.edu" required>
      <div class="note">Note: Only college email IDs will be accepted (e.g., name@college.edu).</div>
      <div id="emailError" class="error"></div>

      <button type="submit">Register</button>
    </form>
  </div>

  <script>
    document.getElementById("registrationForm").addEventListener("submit", function(event) {
      event.preventDefault(); // prevent form submission until validated

      const emailInput = document.getElementById("collegeEmail").value.trim();
      const errorDiv = document.getElementById("emailError");

      // Example: enforce domain to be "@college.edu"
      const allowedDomain = "@college.edu";

      if (!emailInput.endsWith(allowedDomain)) {
        errorDiv.textContent = "Please use your official college email ending with " + allowedDomain;
      } else {
        errorDiv.textContent = "";
        alert("Registration successful! Thank you for providing your college email.");
        // Here you could submit the form data to your server
        // e.g., using fetch() or AJAX
      }
    });
  </script>
</body>
</html>
except KeyboardInterrupt:
    print("\nSniffer stopped.")
API_KEY=THM{super_secret_key12345}
from flask import Flask, render_template_string, request

app = Flask(__name__)

# HTML template embedded directly in Python
form_template = """
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Participant Registration</title>
    <style>
        body { font-family: Arial, sans-serif; background: #f4f6f9; padding: 20px; }
        .container { max-width: 600px; margin: auto; background: #fff; padding: 25px;
                     border-radius: 8px; box-shadow: 0 2px 8px rgba(0,0,0,0.1); }
        h2 { text-align: center; color: #333; }
        label { display: block; margin-top: 15px; font-weight: bold; }
        input { width: 100%; padding: 10px; margin-top: 5px; border: 1px solid #ccc;
                border-radius: 4px; }
        .error { color: red; margin-top: 10px; }
        .success { color: green; margin-top: 10px; }
        button { margin-top: 20px; width: 100%; padding: 12px; background: #0078d7;
                 color: #fff; border: none; border-radius: 4px; font-size: 16px; }
        button:hover { background: #005a9e; }
    </style>
</head>
<body>
    <div class="container">
        <h2>Event Registration</h2>
        <form method="POST">
            <label for="name">Full Name:</label>
            <input type="text" id="name" name="name" placeholder="Enter your full name" required>

            <label for="collegeEmail">College Email Address:</label>
            <input type="email" id="collegeEmail" name="collegeEmail" placeholder="example@college.edu" required>
            <small>Note: Only official college email IDs will be accepted.</small>

            <button type="submit">Register</button>
        </form>
        {% if error %}
            <div class="error">{{ error }}</div>
        {% endif %}
        {% if success %}
            <div class="success">{{ success }}</div>
        {% endif %}
    </div>
</body>
</html>
"""

@app.route("/", methods=["GET", "POST"])
def register():
    error = None
    success = None

    if request.method == "POST":
        name = request.form.get("name").strip()
        email = request.form.get("collegeEmail").strip()

        # Replace with your actual college domain
        allowed_domain = "@college.edu"

        if not email.endswith(allowed_domain):
            error = f"Invalid email. Please use your official college email ending with {allowed_domain}."
        else:
            success = f"Thank you {name}! Registration successful with email {email}."

    return render_template_string(form_template, error=error, success=success)

if __name__ == "__main__":
    app.run(debug=True)
import re
import socket
import ssl
import requests

# Allowed domain for college emails
ALLOWED_DOMAIN = "@college.edu"

def validate_email(email: str) -> bool:
    """
    Validate that the email is in proper format and belongs to the allowed domain.
    """
    pattern = r"^[a-zA-Z0-9._%+-]+@college\.edu$"
    return re.match(pattern, email) is not None

def check_domain_security(domain: str) -> dict:
    """
    Perform a basic cybersecurity check:
    - Verify DNS resolution
    - Check SSL certificate validity
    - Attempt HTTPS connection
    """
    results = {"domain": domain, "dns": None, "ssl": None, "https": None}

    try:
        # DNS resolution
        ip = socket.gethostbyname(domain)
        results["dns"] = f"Resolved to {ip}"
    except Exception as e:
        results["dns"] = f"DNS resolution failed: {e}"

    try:
        # SSL certificate check
        context = ssl.create_default_context()
        with socket.create_connection((domain, 443)) as sock:
            with context.wrap_socket(sock, server_hostname=domain) as ssock:
                cert = ssock.getpeercert()
                results["ssl"] = f"SSL certificate valid, issued to {cert['subject']}"
    except Exception as e:
        results["ssl"] = f"SSL check failed: {e}"

    try:
        # HTTPS request check
        response = requests.get(f"https://{domain}", timeout=5)
        results["https"] = f"HTTPS reachable, status code {response.status_code}"
    except Exception as e:
        results["https"] = f"HTTPS request failed: {e}"

    return results


def main():
    print("=== Cybersecurity Email & Domain Validator ===")
    email = input("Enter your official college email address: ").strip()

    if validate_email(email):
        print(f"✅ Email validated: {email}")
        domain = email.split("@")[1]
        print(f"Performing security checks on domain: {domain}...\n")
        results = check_domain_security(domain)
        for key, value in results.items():
            print(f"{key.upper()}: {value}")
    else:
        print("❌ Invalid email. Please use your official college email ending with @college.edu.")

if __name__ == "__main__":
    main()

